# Docs Debt Radar GitHub Action

This folder is reserved for the GitHub Action wrapper around the CLI.

The V1 Action should:

- Run the scanner in GitHub Actions.
- Support report-only mode by default.
- Support explicit fail thresholds.
- Publish a Markdown job summary.
- Upload report artifacts.
