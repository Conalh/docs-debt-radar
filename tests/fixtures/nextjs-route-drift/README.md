# Next.js Route Drift

Open `/dashboard` after signing in.
Open `/profile` to update your public page.
Open `/settings` to manage the account.
Open `/api/health` for health checks.
Open `/api/status` for Pages Router status checks.
