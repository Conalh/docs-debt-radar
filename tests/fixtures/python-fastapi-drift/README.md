# Python FastAPI Drift

Run `uvicorn app.main:app --reload`.
Endpoint `/health` returns service status.
Endpoint `/api/users` lists users.

Set `DATABASE_URL` before starting the API.
Set `LEGACY_FLAG` to enable the old import path.
